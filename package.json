{
  "name": "esthers-site",
  "private": true,
  "description": "Server-side dependencies for the quote-request API functions. The website itself is plain HTML, CSS and JavaScript with no build step.",
  "dependencies": {
    "@vercel/blob": "^2.8.0"
  }
}
